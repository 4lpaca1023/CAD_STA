# Update these paths to point to your local STA tool binaries.
# Relative paths are resolved against the repository root.

# OpenSTA executable (Tcl shell)
OPENSTA_BIN=OpenSTA/build/sta

# OpenTimer shell executable
OPENTIMER_BIN=OpenTimer/bin/ot-shell

# iEDA iSTA executable
ISTA_BIN=iEDA/build_dynamic/src/operation/bin/iSTA

# Tatum test executable
TATUM_BIN=tatum/build/tatum_test/tatum_test

# Benchmark design (folder under benchmark/designs)
BENCHMARK_DESIGN=simple
